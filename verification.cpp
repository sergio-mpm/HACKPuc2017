#include <iostream>
#include <algorithm>



Bool VerifyPurchase (Bool status){
    if(status==true){
        Verification.final_status=true;
        printf("Pedido Realizado - Finalizando Conexão\n");
        exit(1);
    }
    else
        exit(1);
}
